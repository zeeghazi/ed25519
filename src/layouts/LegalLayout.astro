---
import BaseLayout from './BaseLayout.astro'
import PageHeader from '../components/sections/PageHeader.astro'
import Prose from '../components/ui/Prose.astro'

interface Props {
	title: string
	description: string
	path: string
	updated: string
	illustration?: 'private' | 'fast' | 'standards' | 'mail' | 'chat'
}
const { title, description, path, updated, illustration } = Astro.props
---

<BaseLayout title={title} description={description} path={path} noindex={false}>
	<PageHeader title={title} width="narrow" illustration={illustration} />
	<section class="px-4 pb-24 sm:px-6">
		<div class="mx-auto max-w-[760px]">
			<p class="text-mute font-mono text-xs tracking-wide uppercase">
				Last updated: {updated}
			</p>
			<div class="mt-8"><Prose><slot /></Prose></div>
		</div>
	</section>
</BaseLayout>
